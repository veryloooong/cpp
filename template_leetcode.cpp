#include <algorithm>
#include <vector>

using namespace std;

class Solution {
public:
  int function(vector<int> &nums) {
    sort(nums.begin(), nums.end());
    return 0;
  }
};

int main() { return 0; }